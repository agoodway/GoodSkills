# Publish

Publish a release by running `just publish`.

## Install

Install this skill globally:

```bash
npx skills add agoodway/GoodSkills --skill publish -g
```

Install into the current project:

```bash
npx skills add agoodway/GoodSkills --skill publish
```

## Usage

```
/publish
```
